<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/brazwybor2"
    tools:context=".offical.OfficalListWihTeams">

    <Button
        android:id="@+id/getDataBtn"
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:text="Pobierz listę"
        android:background="@drawable/button_shape"
        android:layout_marginTop="5dp"/>
    <ListView
        android:id="@+id/getDataListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="0dp"
        android:divider="@null"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/getDataBtn"
        android:descendantFocusability="afterDescendants"
        android:layout_marginTop="10dp"/>


    <EditText
        android:id="@+id/getDataIdOfTour"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:textSize="18sp"
        android:inputType="number"
        android:layout_alignBaseline="@id/getDataBtn"/>

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/getDataBtn"
        android:layout_toStartOf="@+id/getDataIdOfTour"
        android:text="ID turnieju:"
        android:layout_marginEnd="3dp"
        android:textSize="20sp"
        android:textColor="#000"
        android:layout_marginTop="5dp"/>

</RelativeLayout>